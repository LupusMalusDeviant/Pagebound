{
  "name": "pagebound-pdf-mcp-server",
  "version": "1.4.0",
  "description": "Tokenless MCP server exposing Pagebound's PDF operations (info, text extraction, text diff, merge, split, extract/delete/rotate/reorder pages, stamp watermark & page numbers, AES-256 encrypt, AcroForm read/fill/create, set metadata, designer documents (create/validate/render Pagebound designs), imagesâ†’PDF) to LLM agents over stdio (local) or Streamable HTTP (hosted). Reuses the same engines as the Pagebound web app: pdf-lib + pdfjs-dist + WebCrypto.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "pagebound-pdf-mcp-server": "dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "clean": "rm -rf dist",
    "smoke": "node dist/smoke.js"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^5.1.0",
    "pdf-lib": "^1.17.1",
    "pdfjs-dist": "^6.0.227",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^6.0.3"
  }
}
