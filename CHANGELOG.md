# Changelog

Alle nennenswerten Änderungen an Pagebound werden in dieser Datei dokumentiert.

Das Format basiert auf [Keep a Changelog](https://keepachangelog.com/de/1.1.0/),
die Versionierung folgt [Semantic Versioning](https://semver.org/lang/de/).

## [Unreleased]

### Added
- Initiale Projekt-Spezifikation: Anforderungsdokument, Lastenheft, Pflichtenheft
- Repo-Skelett: .NET-Solution-Struktur, Lizenz, CI-Skelett, Doku-Standard-Dateien
- Tailwind CSS v4 Setup mit Light/Dark/Sepia-Themes (`wwwroot/css/app.src.css`, `package.json`)
- 8 Service-Interfaces in `Pagebound.Core/Abstractions/`: `IPdfRenderer`, `IAnnotationService`, `ISidecarService`, `IStorageService`, `IHashService`, `IThemeService`, `ILocalizationService`, `ITelemetryService` (Interface-First, ADR-001)
- Domain-Records in `Pagebound.Core/Domain/`: `PdfDocumentHandle`, `Annotation`, `Sidecar`, `LibraryEntry`, `IntegrityRecord`, Enums `HashAlgorithm`/`ThemeName`/`AnnotationType`/`IntegrityStatus`
- Zwei vollständige Service-Implementierungen: `NoOpTelemetryService`, `Sha256HashService`
- i18n-Ressourcen `wwwroot/resources/de.json` und `en.json`
- DI-Registrierungen in `Program.cs` gegen Interfaces
- **PDF.js-Integration**: TypeScript-Bridge `wwwroot/js/pdfjs-bridge.ts` (esbuild-Bundle als IIFE-Global `pageboundPdf`), Worker-Datei `pdf.worker.min.mjs` aus pdfjs-dist
- **`PdfJsRenderer`** in `Pagebound.Infrastructure/Pdf/`: erste Implementation von `IPdfRenderer` (`LoadAsync`, `RenderPageAsync`, `UnloadAsync` vollständig; `ExtractTextAsync`/`SearchAsync`/`GetOutlineAsync` als `NotImplementedException` mit TODO)
- `Microsoft.JSInterop` (10.0.8) als Paket-Referenz in `Pagebound.Infrastructure`
- **Reader-Demo-Page** unter `/reader`: File-Picker, Seitennavigation, Zoom-Stufen (75/100/150/200 %)
- `esbuild.mjs` + `tsconfig.json` für JS-Interop-Bundling
- npm-Scripts erweitert: `build` (CSS+JS), `build:js`, `watch:js`, `typecheck`
- CI-Workflow nutzt `npm run build` (zuvor nur `build:css`)
- Navigation in `MainLayout` mit `NavLink`-Items (Start, Reader)
- **Docker-Setup für lokales / Self-Hosted-Deployment**: Multi-Stage `Dockerfile` (Node-Stage baut Tailwind+esbuild, .NET-10-Stage `dotnet publish`, `nginx:alpine`-Stage liefert statische Site), `infra/docker/nginx.conf` mit korrekten MIME-Types für WASM/PWA/PDF.js und SPA-Fallback auf `index.html`, `.dockerignore` für schlanken Build-Kontext

### Fixed
- Reader-Layout nutzt jetzt die volle Breite des Hauptbereichs — Controls (Header, File-Picker, Navigation, Zoom-Buttons) bleiben auf `max-w-4xl` mittig zentriert, das PDF-`<figure>` nimmt den verfügbaren Platz bis `max-w-[1800px]`. Zoom-Faktoren neu gemappt: der angezeigte „100 %"-Default entspricht jetzt einem internen PDF.js-Scale von 2.0 (vorher 1.5), Stufen jetzt 50 / 75 / 100 / 125 / 150 / 200 %.
- Such-Eingabe bindet jetzt auf `oninput` statt nur `onchange` — Suchen-Button wird sofort beim Tippen aktiv, kein Klick aus dem Feld mehr nötig.
- `buildPageText` in der PDF.js-Bridge fügt einzelne Glyphen wieder zu Wörtern zusammen, statt sie naiv mit Leerzeichen zu trennen. Bei stilisiert gerenderten PDFs (Item-pro-Glyph durch Hand-Positionierung) findet die Suche jetzt zuverlässig.

### Implemented
- **Wasserzeichen + Seitenzahlen/Bates** (Tools, Richtung 1.0): Neue „Stempeln"-Karte auf `/tools` — ein diagonales Text-**Wasserzeichen** (halbtransparent, 45°) und/oder **Seitenzahlen** (Format `{n} / {total}`, Position unten links/mitte/rechts) auf jede Seite, in einem Durchlauf via pdf-lib (`stampPdf`-Bridge + `IPdfManipulator.StampAsync` + `StampOptions`). Kein Re-Rendering, Eingabe bleibt unverändert (Download `<name>.stamped.pdf`). Im Browser end-to-end verifiziert (2-Seiten-PDF → gültiges, größeres PDF). i18n `tools.stamp.*`.
- **MCP-Server für LLM-Agenten** (`mcp/`, `pagebound-pdf-mcp-server`): Pagebounds PDF-Engine als **tokenloser stdio-MCP-Server** — ein LLM-Agent kann dieselben Operationen nutzen wie die Web-App, ohne Auth/Login. Reuse derselben Engines (pdf-lib + pdfjs-dist), keine nativen Abhängigkeiten, kein Netzwerk, Datei­pfad-basiertes I/O (kleine Tool-Ergebnisse). **8 Tools**: `pdf_info`, `pdf_extract_text` (Text-Layer, keine OCR), `pdf_merge`, `pdf_extract_pages` (auch zum Splitten), `pdf_delete_pages`, `pdf_rotate_pages`, `pdf_reorder_pages`, `images_to_pdf` — alle mit Zod-Validierung, Annotations (read-only/destructive/idempotent) und strukturiertem Output. Schreibende Tools erzeugen neue Dateien (Eingaben unangetastet). Selbsttest (`npm run smoke`) + MCP-Handshake (`initialize`/`tools/list`) grün; eigener CI-Job (build + smoke). Verschlüsselung/OCR/PNG-Rendern bewusst ausgelassen (bräuchten WebCrypto-Port/Tesseract/Canvas) — als Erweiterung dokumentiert. Anbindung + Beispiele in [`mcp/README.md`](mcp/README.md).
- **Responsive-Fix + GitHub-Link**: Auf schmalen Viewports sprengte die nicht-schrumpfende Running-Head („Pagebound — START") zusammen mit Theme-Toggle/Version die Topbar → horizontaler Overflow (Doc 441 px bei 375 px Viewport). Jetzt: ≤620 px nur noch der Crumb in der Topbar (Präfix + Version raus, kompakteres Padding, schmalere `.ed`-Ränder), ≤400 px zusätzlich der Theme-Schnellschalter aus (Theme bleibt im Einstellungs-Panel). Verifiziert: **kein Overflow** mehr auf Mobile (375)/Tablet (768)/Desktop, Rail-Drawer + Scrim funktionieren. **GitHub-Link** im Rail-Footer (neuer `github`-Icon, `target=_blank rel=noopener noreferrer`) → `github.com/LupusMalusDeviant/Pagebound`.
- **Deployment (Infomaniak-Host, neben lupusmalus.dev)**: Pagebound deployt als statische WASM-App (nginx-Container) hinter dem dort laufenden **Caddy**-Reverse-Proxy auf dem gemeinsamen Docker-Netz `lupusmalus-net`, erreichbar unter **pagebound.app.lupusmalus.dev** (Caddy-Auto-TLS). Neuer GitHub-Actions-Workflow `publish-image.yml` baut das Image und pusht `ghcr.io/lupusmalusdeviant/pagebound:latest` (gleiches GHCR-Muster wie die anderen Dienste); `infra/deploy/` enthält Compose, Caddy-Site-Block (nur HSTS — der Blazor-CSP kommt aus dem Image, keine CSP-Kollision) und ein idempotentes `deploy.sh` (Image ziehen → Container hoch → Caddy-Block einhängen mit Validate+Rollback). **Dockerfile-Fix:** das Image kopierte nur 4 der 9 JS-Bridges aus dem Build-Stage — im sauberen CI-Checkout fehlten Manipulator/OCR/Files/Split/Workspace/Tweaks (App halb tot). Jetzt werden alle Bundles übernommen, `.dockerignore` ignoriert alle generierten `wwwroot/js/*.js` → reproduzierbarer Build nur aus Quellen.
- **Settings-Politur**: Erklärzeile unter „Bewegung erlauben" ("Reduziert Animationen & Übergänge …"), damit der Reduce-Motion-Schalter selbsterklärend ist.
- **Beta-Stand + UI-Politur**: Version auf **0.8 Beta** angehoben, alle „Pre-Alpha"-Marker entfernt (Topbar `v0.8.0-beta`, Rail-Badge „Beta", Home-Folio „Open Source"). **Schiene klappt nicht mehr automatisch ein**, wenn man Reader/Split öffnet (auf Nutzerwunsch — auf schmalen Viewports bleibt der ☰-Off-Canvas-Modus). **Vier neue Akzentfarben** (Violett, Blau, Rosé, Bernstein → jetzt 8), alle in beiden Themes WCAG-AA-geprüft (Akzent-Text ≥4,7:1 auf der Schiene, Button-Label ≥5,5:1). **Pfeil-Annotation**: der Schaft endet jetzt an der Spitzen-Basis statt am Endpunkt — die runde Strich-Endkappe verschwindet unter dem Dreieck, kein „Punkt" mehr an der Spitze (Vorschau + gespeichertes Rendering gleich).
- **Benutzerhandbuch** (Doku, Richtung 1.0): Vollständiges deutsches Endnutzer-Handbuch unter [`docs/05-benutzerhandbuch.md`](docs/05-benutzerhandbuch.md) — Erste Schritte, Reader, Suche & OCR, Annotationen, Formulare, Bibliothek, Split-Ansicht, Seiten-Werkzeuge, Bilder→PDF, Konvertieren, Verschlüsselung, Stapelverarbeitung, Workspace/Sidecar, Einstellungen, **Tastaturkürzel** (an der echten Shortcuts-Bridge verifiziert), Datenschutz/Sicherheit und Browser-Grenzen. Aus dem README verlinkt. Damit ist der **1.0-Inhalt vollständig**; offen bleibt nur die Release-Mechanik (Tag + Repo öffentlich).
- **M4 Code-Coverage ≥ 60 %** (NFA, Richtung 1.0): Coverage von **27 % auf 75 %** gehoben (Core 70 %, Infrastructure 82 %). **(a)** `coverlet.runsettings` nimmt die zehn reinen **JS-Interop-Shims** aus der Messung (PdfJsRenderer, JsPdfLibManipulator, JsPdfEncryptor, JsPdfConverter, JsImageToPdfConverter, TesseractOcrService, IndexedDbStorage, FileSystemAccessHandleService, BrowserWorkspaceService, ThemeService) — dünne `IJSRuntime`-Wrapper, deren Logik im Browser läuft und über die E2E-/Browser-Verifikation abgedeckt ist (ein gemocktes `IJSRuntime` würde nur Aufruf-Argumente nachstellen). **(b)** Echte Unit-Tests für die bislang ungetestete Logik: `LibraryService` (Add/Merge per Id+Hash, Volltext-/Tag-Filter, Sortierung, Paginierung, Tag-Aggregation, FindByHash, Touch, Remove — gegen ein In-Memory-`IStorageService`), `AnnotationService` (Create/Update/Delete/Get, Persistenz über Instanzen, Fehlerpfade), `LocalizationService` (Bundle-Laden, `T()`-Interpolation/Fallback, Sprachwahl — gegen gemocktes `IJSRuntime` + Stub-`HttpClient`) und die Domain-Helfer `InkAnnotation`/`ShapeAnnotation`. **+54 Tests, Core-Suite 79 → 133**, alle grün. CI (`build-and-test`) und CONTRIBUTING nutzen die Runsettings; Codecov spiegelt damit die testbare Fläche. Grundsatz dokumentiert: echte Logik wird getestet, nicht ausgeschlossen.
- **M5-E Design-Feinschliff: einheitliche Button-Sprache** (Richtung 1.0): Die primären Aktionen aller Funktions-Screens (Werkzeuge: Bilder wählen/Bauen/Speichern; Stapel: Ausführen; Library: Workspace wählen/Leer-CTA; Reader: PDF öffnen/Suchen/OCR/Signatur merken/Formular speichern/Notiz speichern) liefen als rohe `bg-pb-accent`-Pillen mit **weißem** Text. Jetzt durchgängig das editoriale `.btn primary`-Primitive (Akzent + dunkle `accent-ink`-Schrift, redaktionelle Polsterung/Radien) — wie schon auf der Startseite, und in beiden Themes WCAG-AA-korrekt (Kontrast aus M4). Auch die nativen Datei-Picker-Buttons (`::file-selector-button`) auf `accent-ink` umgestellt, damit die Button-Sprache lückenlos konsistent ist. Im Browser in Hell + Dunkel verifiziert. Der reicher gebaute Reader-Toolbar-Stack bleibt bewusst als Panel-Layout (nicht in die schlankere Mockup-`.reader-toolbar` gezwängt) — die Funktionsfülle hat Vorrang.
- **M4 E2E-Harness lauffähig** (NFA-072, Richtung 1.0): Die Playwright-Smoke-Tests (`AppLaunchTests`) waren rot — sie zielten auf `:5000`, starteten keinen Server und brauchten Browser-Binaries. Jetzt eine geteilte `WebAppFixture`: pollt die Ziel-App (`PAGEBOUND_URL`, Default `:5099`), stellt Chromium per `playwright install` sicher und startet ihn — fehlt Server oder Browser, **überspringen** sich die Tests (`[SkippableFact]`) statt zu scheitern. So bleibt `dotnet test` **per Default grün** und läuft trotzdem echt durch, sobald die App erreichbar ist. Assertion an die editoriale Startseite angepasst (Reader-CTA-Anker statt `<button>Öffnen</button>`), Shell-Wait statt `NetworkIdle` (Dev-Server hält Live-Reload-Socket offen). Im Browser verifiziert: gegen Live-App **3/3 grün**, ohne Server **3 übersprungen** (0 Fehler) — Gesamt-Suite **82/82** (79 Core + 3 E2E). CI: `build-and-test` schließt E2E aus (`--filter`), der dedizierte `e2e-tests`-Job fährt sie mit echtem Server.
- **M4 A11y-Audit (WCAG 2.1 AA)** (Richtung 1.0): Harter Barrierefreiheits-Durchgang, im Browser gegen den Live-Preview gemessen. **(a) Kontrast (1.4.3 / 1.4.11):** Die oklch-Tonleiter neu gespreizt — alle Textpaare erreichen jetzt **≥4,5:1 in beiden Themes** (dunkel + hell), inkl. der zuvor zu blassen `--ink-4`-Stufe und der Sonderfälle „Text auf der Schiene" (Rail liegt auf `--bg-sunken`). Fürs **helle** Theme wurden `--accent`/`--ok`/`--warn` abgedunkelt, damit Buttons-Labels (`accent-ink` auf `accent`), Links und `.status`/`.priv`-Text die 4,5:1 treffen. Neuer Token `--ctl-border` (≥3:1) für **Eingabefelder/Toggle/Segmente**, sodass ein leeres Feld als Bedienelement erkennbar ist (1.4.11) — dekorative Haarlinien-Trenner bleiben fein. Bewegung respektiert `prefers-reduced-motion` **und** den manuellen Schalter. **(b) Tastatur/Fokus/ARIA (2.1.1 / 2.4.1 / 2.4.3 / 4.1.2):** **Skip-Link** „Zum Inhalt springen" als erstes fokussierbares Element (→ `#main`). **Settings-Dialog** als vollständige Disclosure: Zahnrad mit `aria-haspopup`/`aria-expanded`/`aria-controls`, `role="dialog"`, Fokus wandert beim Öffnen hinein, **Escape schließt**, Fokus kehrt zum Auslöser zurück (live verifiziert). Off-Canvas-Rail wird bei geschlossenem Zustand `visibility:hidden` → **nicht mehr in der Tab-Reihenfolge** (kein Fokus auf unsichtbare Items). Fehlende Namen ergänzt: Library-Sortierung & Batch-Operation `<select>`, generierte PDF-Formularfelder (`aria-label`=Feldname). Sweep über alle Routen: keine namenlosen Controls, kein fehlendes `alt`, keine Heading-Sprünge, kein positives `tabindex`, keine doppelten IDs. Close-Glyph `✕` → SVG-`<Icon>`. Core-Tests 79/79.
- **M6 Security-Check** (Richtung 1.0): Harter Security-Durchgang. **CVEs:** `PdfSharpCore` + dessen transitive `SixLabors.ImageSharp` 1.0.4 (High-Severity NU1903) komplett entfernt — war seit M1 toter Code im Web-Pfad; `dotnet build` jetzt NU19-frei. **Krypto-Audit** (WebCrypto AES-256 R6): alle Zufallswerte aus `crypto.getRandomValues` (CSPRNG), Algorithm 2.B + Key-Derivation korrekt (ISO 32000-2 §7.6.4.3), Self-Test vorhanden — keine Defekte; dokumentierte Grenzen (nur Streams verschlüsselt, kein SASLprep). **XSS/Parsing:** PDF.js `isEvalSupported:false`, Markdig `DisableHtml()` (kein Roh-HTML, einzige `MarkupString`-Senke ist die sanitisierte Markdown-Ausgabe). **HTTP-Header:** CSP + `X-Frame-Options`/`X-Content-Type-Options`/`Referrer-Policy`/`Permissions-Policy` in der nginx-Config (auf Blazor-WASM `wasm-unsafe-eval` + Tesseract-OCR-CDN abgestimmt). Neue `SECURITY.md` mit Threat-Model + Findings. Core-Tests 79/79.
- **M5 „Warm Ink"-Redesign** (Richtung 1.0, UI-Politur): Komplettes visuelles Redesign nach dem Claude-Design-Entwurf. **(a) Fundament:** warm-dunkles oklch-Token-System (dark + light) + **self-gehostete Fonts** (Newsreader/Hanken Grotesk/JetBrains Mono als woff2 in `wwwroot/fonts` — kein Google-Fonts-Request, NFA-020), Papier-Grain; die bestehenden Tailwind-`pb-*`-Tokens sind auf Warm Ink gemappt, sodass die ganze App den Look sofort übernimmt. **(b) Shell + Settings:** neue editoriale App-Shell (nummerierte Rail 01–05, `Page`*`bound`*-Masthead, „Sammlung", Privacy-Footer; Running-Head-Topbar mit Theme-Toggle, Sprache, Version) + **Settings-Bereich** — das „Tweaks"-Panel als eigener Bereich (Zahnrad): Theme Dunkel/Hell, Akzentfarbe (teal/jade/aqua/coral), Schriftgröße, Dichte (kompakt/normal/luftig), Bewegung; via neuer `pageboundTweaks`-Bridge live auf CSS-Variablen (`data-theme`/`--accent-h/c`/`--font-scale`/`--d`/`data-motion`) + localStorage, Pre-Boot-Apply ohne FOUC. **(c) Screens:** editoriale Home (Wordmark, USP-TOC, Roadmap-Ledger, Margin-Rail) + editoriale Header (Folio + Serif-`page-title` + Standfirst) auf Werkzeuge/Library/Batch über der unveränderten Funktionalität. Stubs raus: „Tabs — folgt", „Reader (Demo)", die Skelett-Subheading. Der alte Glassmorphism + ThemeService-Zyklus (Auto/Sepia) entfällt. Im Browser durchverifiziert (Tweaks wirken live + persistent, FA-064 greift, alle Routen rendern), Tests 82/82. **(d) Icons + Logo:** Emoji-Glyphen app-weit durch einen SVG-Icon-Satz (`Icon.razor`, Design-Linien-Icons) ersetzt — i18n-Labels emoji-frei, `<Icon>` auf den prominenten Buttons. Das Pagebound-Logo (`src/Icons/Pagebound_Light`) als `favicon.ico` (Browser-Tab) + Marken-Plakette in Rail-Masthead & Splash.
- **FA-051 Stapelverarbeitung** (Release 0.9): Neue Seite `/batch` — mehrere PDFs auf einmal wählen und eine Operation(skette) anwenden: **Komprimieren**, **Verschlüsseln** (ein Passwort für alle), **Komprimieren + Verschlüsseln** (verkettet) oder **→ Text**. Alle Ergebnisse landen in einer ZIP (`pagebound-batch.zip`, eine Ausgabe je Eingabe, mit Namens-Dedup), gepackt über die neue Bridge-Funktion `pageboundPdf.zipFiles` (fflate). Orchestriert die bestehenden Services (`IPdfManipulator`/`IPdfConverter`) — keine Doppel-Logik. Nav-Eintrag „Stapel". Im Browser verifiziert (2 PDFs → Komprimieren → `pagebound-batch.zip` geladen). Gespeicherte Regeln (FA-052, Could) folgen.
- **FA-030/031/032 Konvertierungen PDF→PNG/JPG/Text/HTML** (Release 1.0): Neue „Konvertieren"-Leiste auf `/tools` wandelt das geladene PDF um — **PNG/JPG** (je Seite ein Bild, als ZIP via fflate gepackt), **reiner Text** (.txt, Seiten per Form-Feed getrennt) und **HTML** (jede Seite als eingebettetes PNG → pixel-genau + offline öffenbar). Neue Bridge-Funktionen `pageboundPdf.convertToText` / `convertToImagesZip` / `convertToHtml` (Seiten-Rendering bzw. Text-Extraktion über PDF.js) + `IPdfConverter`/`JsPdfConverter` + DI; Download über `downloadBytes`. Im Browser verifiziert: PNG-ZIP (gültiges PK-Archiv mit `seite-1.png`/`seite-2.png`), JPG-ZIP, HTML (2 eingebettete Seiten-Bilder), Text (String). Neue Dependency `fflate` (store-only ZIP). i18n `tools.convert.*`.
- **FA-064 Auto-Collapse-Sidebar** (Release 0.5): Die Navigations-/Library-Sidebar klappt im Lesemodus (Reader, Split-View) automatisch ein und auf den übrigen Routen wieder aus; ein ☰-Button im Header blendet sie jederzeit manuell ein/aus (ab md-Breite). i18n `nav.sidebar.toggle`. Im Browser verifiziert (Home: 256 px sichtbar → /reader: eingeklappt → Toggle: wieder sichtbar).
- **FA-072/073 Zentraler Sidecar-Workspace** (Release 0.5): Optionaler zentraler Ordner für Sidecar-Dateien, entkoppelt vom PDF-Speicherort — über die File-System-Access-Directory-API (`showDirectoryPicker`, Chromium). Das gewählte Verzeichnis-Handle wird in IndexedDB persistiert (übersteht Reloads, dezenter Permission-Re-Prompt pro Session). Sidecars liegen als `{pdfHash}.pagebound.json`; beim Öffnen einer PDF wird der Workspace automatisch geprüft (FA-073) und neue Annotationen zusammengeführt (Id-Dedup verhindert Duplikate beim Wieder-Öffnen). Neuer `IWorkspaceService`/`BrowserWorkspaceService` + Bridge `workspace-bridge.ts` (globalName `pageboundWorkspace`); Workspace-Leiste auf `/library` (Ordner wählen/ändern/entfernen) + „💾 In Workspace"-Button im Reader. Auf Browsern ohne die API (Firefox/Safari) unsichtbar — Fallback bleibt der Download/Upload-Flow. Bridge-Round-Trip im Browser gegen OPFS verifiziert (save → load → missing→null → clear); der native Verzeichnis-Picker selbst braucht eine Nutzer-Geste (wie der bestehende Datei-Picker).
- **FA-061/062 Library-Ansichten Tabelle + Raster** (Release 0.5): Die Library-Seite bietet jetzt drei umschaltbare Ansichten — **Liste** (Default, volle Tag-Bearbeitung), **Tabelle** (Spalten Titel/Seiten/Größe/Tags/Hinzugefügt/Zuletzt geöffnet) und **Raster** (Karten). Alle teilen sich Öffnen/Entfernen/Tag-Chips; das Tag-*Hinzufügen* bleibt in der Liste. Toolbar-Umschalter + neue i18n-Schlüssel `library.view.*` / `library.col.*`. Im Browser gegen drei Seed-Einträge verifiziert — alle drei Ansichten rendern korrekt (Liste/Tabelle/Raster), Umschalten funktioniert.
- **FA-092 Split-View: Resize + Sync-Scroll** (Release 0.6): Die Trennlinie zwischen den beiden Reader-Panes ist jetzt per Maus/Touch **ziehbar** (Pointer-Capture-Drag, auf 15–85 % Breite begrenzt) und ein optionaler Schalter **„Synchron scrollen"** spiegelt die Scroll-*Fraktion* proportional zwischen den Panes (unterschiedlich lange Dokumente bleiben im Takt) — mit Re-Entrancy-Guard gegen Scroll-Ping-Pong und sauberem Teardown bei Deaktivieren/Navigation. Neue Bridge `split-bridge.ts` (globalName `pageboundSplit`: `initResize` / `setSyncScroll` / `teardownResize`), in `esbuild.mjs` + `index.html` registriert. i18n `split.resize.*` ergänzt. Im Browser verifiziert: Gutter zeigt `col-resize`, Drag setzt die Pane auf 30 %, Scroll-Spiegelung proportional (Fraktion 0,5 → 0,5), Teardown stoppt das Mitspiegeln.
- **FA-027 PDF-Verschlüsselung mit AES-256** (Release 0.8): rein **managed** implementiert (ISO 32000-2 `/V 5` `/R 6`), nutzt nur SHA-256/384/512 + AES — **kein MD5**, daher WASM-tauglich (PdfSharpCores eigene Verschlüsselung crasht in WASM am MD5, siehe ADR-004). Neuer Krypto-Kern `AesR6` (Algorithm-2.B-Hash, Key-Setup `/U /UE /O /OE /Perms`) + `IPdfEncryptor`/`ManagedPdfEncryptor`: PdfSharpCore normalisiert die Eingabe (`NormalizeAsync`), dann verschlüsselt `PdfAesEncryptor` die Stream-Daten objektweise (AES-256-CBC, IV-prefixed) und schreibt `/Encrypt` + xref neu. `JsPdfLibManipulator.EncryptAsync` delegiert dorthin; `/tools` bekommt eine Passwort-Leiste (Download `<name>.encrypted.pdf`). **MVP-Grenze:** nur Streams (Seiteninhalt) werden verschlüsselt (`/StmF /StdCF`), Strings bleiben Klartext (`/StrF /Identity`) — volle String-Verschlüsselung folgt. Der vorherige RC4-Ansatz (PdfSharpCore, nur Desktop) wurde entfernt. Selbst-Konsistenz-Tests (Round-Trip Verschlüsseln→Key-Recovery→Entschlüsseln). **Update (Browser-Test 2026-05):** Der managed AES-Pfad fror den Single-Thread-WASM ~30 s ein (kein Hardware-AES). Daher auf **WebCrypto** umgestellt (`pageboundPdfManipulator.encryptPdf` + `JsPdfEncryptor`): hardware-beschleunigtes AES, **~15 ms**, im Browser **gegen PDF.js verifiziert** (öffnet mit korrektem Passwort, weist falsches ab). UI wieder aktiv. Der managed `AesR6`/`PdfAesEncryptor` bleibt als unit-getestete Algorithmus-Referenz erhalten. Zusatz: ein Worker-Shim (`pdfjs-worker-shim.mjs`) polyfillt `Math.sumPrecise`, das pdfjs-dist 5.7 voraussetzt — sonst crasht PDF.js in Browsern ohne dieses sehr neue API.
- **FA-020..024 PDF-Manipulation jetzt WASM-tauglich**: Merge/Split/Reorder/Delete/Rotate liefen über `PdfSharpManipulator` und crashten in Blazor WASM (PdfSharpCore.Save → `MD5.Create()` via CryptoConfig-Reflection → `TargetInvocationException`) — sie hatten nur die Desktop-Unit-Tests bestanden. Komplett auf pdf-lib umgestellt (`mergePdfs`/`splitPdf`/`reorderPdf`/`deletePages`/`rotatePages`/`normalizePdf` in der Bridge); im Browser verifiziert. PdfSharpCore ist damit aus dem Web-Pfad raus.
- **FA-025 Bilder → PDF** (Release 0.8): aus PNG/JPG-Bildern eine PDF erzeugen — je Bild eine Seite, Reihenfolge per Drag & Drop, Seitengröße wählbar (Bildgröße / A4 / Letter, Bild „contain"-eingepasst). Neuer `IImageToPdfConverter`/`JsImageToPdfConverter` + Bridge-Funktion `pageboundPdfManipulator.imagesToPdf` (pdf-lib `embedPng`/`embedJpg`); Bilder werden als Base64 übergeben (verschachtelte `byte[]` marshallt Blazor nicht als `Uint8Array`). „Bilder → PDF"-Karte auf `/tools` (immer sichtbar, Multi-Select, Vorschau, Download `bilder.pdf`). Browser-Verifikation der Bild-Einbettung steht noch aus.
- **FA-005 Volltext-Suche** (erste Iteration): Eingabefeld in `ReaderPage` mit Optionen „Groß-/Kleinschreibung" und „Ganzes Wort", Trefferliste mit Snippet (Match hervorgehoben), Klick auf Treffer springt zur Seite. `IPdfRenderer.ExtractTextAsync` und `IPdfRenderer.SearchAsync` sind jetzt vollständig implementiert; JS-Bridge führt seitenweise `getTextContent()`, baut robusten Page-Text und liefert Treffer inkl. Snippet zurück.
- Wenn eine geöffnete PDF keinen Text-Layer enthält (typische Designer-PDFs mit Text-als-Vektor), erscheint statt stummer „Keine Treffer" ein klarer Hinweisblock mit Verweis auf das geplante OCR-Feature (FA-050 / Release 0.9).
- `SearchHit`-Record erweitert um `Snippet` und `SnippetMatchStart` (Item-genaue `TextItem[]`-Treffer für Highlight-Overlays bleiben Folge-Iteration).
- **Tastatur-Navigation** (Beitrag zu NFA-031, „vollständige Tastatur-Navigation"): neue Bridge `shortcuts-bridge.ts` (globalName `pageboundShortcuts`) registriert einen window-keydown-Listener und mapped Shortcuts auf `[JSInvokable]`-Aufrufe von `ReaderPage`. Aktive Shortcuts: `←` / `Pg↑` vorherige Seite · `→` / `Pg↓` nächste Seite · `Pos1` / `Ende` erste / letzte Seite · `Strg+F` Suchfeld fokussieren · `Esc` Suche zurücksetzen · `Strg+ +` / `Strg+ −` Zoom-Stufe · `Strg+0` Zoom-Reset. Beim Tippen in Input-Feldern werden alle Shortcuts außer `Esc` und `Strg+F` unterdrückt. esbuild bündelt jetzt zwei Bridges (`pdfjs-bridge.js`, `shortcuts-bridge.js`); `index.html`, Dockerfile und `.gitignore` entsprechend erweitert.
- **Persistenz-Infrastruktur**: neue Bridge `storage-bridge.ts` (globalName `pageboundStorage`) kapselt die Browser-IndexedDB; `IndexedDbStorage` in `Pagebound.Infrastructure/Storage/` implementiert `IStorageService` über die Bridge (JSON-Serialisierung mit camelCase, Drittes esbuild-Bundle). Erfüllt NFA-010 (Offline) und NFA-011 (sofortige Persistenz). `IStorageService.KeysAsync` um optionalen `CancellationToken` erweitert.
- **`AnnotationService`** in `Pagebound.Infrastructure/Annotations/` implementiert `IAnnotationService` mit per-PdfId-Cache und sofortiger Persistenz via `IStorageService` unter dem Schlüssel `annotations:{pdfId}`. Operationen (`CreateAsync`, `UpdateAsync`, `DeleteAsync`, `GetForDocumentAsync`) sind durch `SemaphoreSlim` linearisiert.
- **FA-011 Sticky Notes**: Werkzeug-Toggle in `ReaderPage` zwischen „Auswählen" und „📝 Notiz". Im Notiz-Modus erzeugt ein Klick aufs PDF einen Pin an der genau geklickten Position. Pins werden als kleine farbige Buttons über dem PDF-Bild gerendert (Position als Fraction 0..1, damit zoom-stabil) und öffnen beim Klick einen Editor-Popover mit Textarea, Speichern- und Löschen-Knopf.
- **PDF-Hash bei Datei-Auswahl**: `Sha256HashService` wird auf den Datei-Stream angewendet; das Ergebnis ist die `PdfId`, unter der die Annotationen persistiert werden. Damit folgen die Notizen der PDF, auch wenn sie umbenannt oder kopiert wird.
- Neuer DOM-Utility `pageboundShortcuts.clientPositionToFraction(selector, clientX, clientY)` für zoom-stabile Klick-Positionen.
- **FA-010 Text-Highlight** und **FA-018 Inline-Toolbar bei Text-Selektion**: Über dem PDF-Bild wird ein unsichtbarer Text-Layer aus `IPdfRenderer.ExtractTextAsync`-Items aufgespannt (positionierte transparente `<span>`s, Größen über CSS-Container-Query-Einheiten `cqh`, Fractions 0..1). Markiert der Nutzer Text mit der Maus, wird die Browser-`Selection` per neuer Bridge-Funktion `pageboundShortcuts.getCurrentTextSelection(selector)` in eine Liste von Bounding-Rechtecken (relativ zum Page-Canvas) umgewandelt; eine schwebende **Inline-Toolbar** mit fünf Farb-Buttons erscheint und legt bei Klick eine neue `HighlightAnnotation` an. Bestehende Highlights rendern als farbige Rechtecke mit `mix-blend-mode: multiply` über dem Text-Layer und öffnen beim Klick einen Editor-Popover (Farbwechsel, Löschen). `RenderedPage` um `PageWidthPt`/`PageHeightPt` erweitert, neue Helfer `HighlightAnnotation` und `HighlightRect` im Domain-Layer. `Esc` priorisiert jetzt zuerst aktive Selection/Editor vor dem Such-Reset.
- **FA-006 Inhaltsverzeichnis-Sidebar** (Release 0.2 vorgezogen): neue Bridge-Funktion `pageboundPdf.getOutline(handleId)` löst PDF.js-Destinations (sowohl inline-Arrays als auch Named Destinations) via `getPageIndex` zu Seitenzahlen auf. `IPdfRenderer.GetOutlineAsync` ist jetzt implementiert. Neue rekursive Komponente `OutlineNode.razor` rendert den Tree mit eingerückten Kindern; eine einklappbare Sidebar links vom PDF zeigt das Inhaltsverzeichnis und springt bei Klick zur Zielseite. Toggle-Button „📑 Inhalt" im Werkzeug-Bereich, mit „(leer)"-Hinweis wenn die geöffnete PDF kein TOC hat.

- **FA-012 Markdown-Notizen mit Live-Preview**: Der Sticky-Note-Editor hat jetzt einen Toggle „Bearbeiten ⇄ Vorschau". Beim Öffnen einer befüllten Notiz startet der Editor im Vorschau-Modus; eine leere neue Notiz startet im Bearbeiten-Modus. Die Vorschau rendert via Markdig (`UseAdvancedExtensions().DisableHtml()` → kein roher HTML-Durchschuss, NFA-025) und wird über die neue Tailwind-Klasse `pb-markdown` themed (Headings, Listen, Code-Blöcke, Blockquotes, Tabellen, Links). Editor-Popover ist von 18 rem auf 20 rem verbreitert, damit Vorschau und Markdown-Eingabe komfortabel reinpassen.
- **FA-080 / FA-081 / FA-082 Markdown-Export** (Anti-Adobe-USP, Obsidian-kompatibel): neuer Service `IMarkdownExporter` in `Pagebound.Core.Abstractions` + `MarkdownExporter` in `Pagebound.Infrastructure.Export` sammelt Highlights und Sticky Notes pro PDF (per `PdfId`), gruppiert sie nach Seite, sortiert innerhalb einer Seite nach Y-Position und schreibt YAML-Frontmatter (`title`, `source` als optionaler Wikilink, `pdfHash`, `pages`, `annotations`, `exportedAt`, `exportedBy`, `tags`). Highlights werden als Blockquote-Zitate ausgegeben, Sticky Notes mit ihrem Markdown-Inhalt unterhalb eines `**Notiz:**`-Headers. Neuer JS-Helfer `pageboundShortcuts.downloadFile(filename, content, mimeType)` liefert die generierte `.md`-Datei als Browser-Download (Blob + Object-URL, sauber aufgeräumt). „📤 Markdown"-Button in der Reader-Werkzeugleiste mit Annotation-Anzahl-Badge.
- **FA-015 / FA-016 / FA-017 PNG-Signatur + Hash-Integrität** (Original-USP, Release 0.4 vorgezogen): neuer `SignatureAnnotation`-Helper im Domain-Layer kapselt PNG-Data-URL + 0..1-Fraction-Position + Signaturzeitpunkt + Integritäts-Hash. Neues Interface `IIntegrityService` (Abstractions) und `IntegrityService` (Infrastructure/Crypto) berechnen einen deterministischen SHA-256 über pdfHash, zeitstempel und alle **anderen Annotationen außer Signaturen** in einem kanonisch sortierten String — Signaturen ignorieren sich gegenseitig, damit das Verschieben einer Signatur keine andere Signatur invalidiert. Inhaltsänderungen (Sticky Notes / Highlights anlegen, ändern, löschen) lösen automatische Reverifikation aus und switchen den Status-Badge der Signatur von Grün auf Rot. UI: Werkzeug „✍️ Signatur laden" lädt PNG/JPG bis 2 MB als Data-URL in den Modus, Klick auf das PDF platziert die Unterschrift (25 % × 8 % Default-Größe), Drag-and-Drop verschiebt sie live (`pageboundShortcuts.dragElementToFraction` schreibt während pointermove direkt CSS-`left`/`top` und meldet die finale Fraction über `[JSInvokable] HandleSignatureDragCompleteAsync` zurück; Position wird auf das Page-Canvas geklemmt). Mini-Lösch-Button am Signatur-Rahmen, kleines Farb-Badge oben rechts (✓ Valid, ! Invalid, ? NoHash). UI weist auf die pragmatische Variante laut ADR-006 hin (keine PAdES, keine PDF-Modifikation).
- **FA-020 / FA-021 / FA-022 PDF-Seiten-Werkzeuge** (Acrobat-Pro-Parität, Release 0.3): neue `IPdfManipulator`-Abstraktion in `Pagebound.Core.Abstractions` + `PdfSharpManipulator`-Implementation in `Pagebound.Infrastructure.Pdf` mit `MergeAsync`, `SplitAsync`, `ReorderAsync`, `DeletePagesAsync`, `RotateAsync` über PdfSharpCore; `CompressAsync` und `EncryptAsync` aktuell als `NotImplementedException` für Release 0.8. Domain-Records `CompressionOptions` und `EncryptionOptions` (inkl. `EncryptionStrength`) angelegt. Neue Page `/tools` mit File-Picker, sequenziellem Thumbnail-Rendering (Scale 0.35, Progress-Bar), Seiten-Grid mit Selection (Single + Alle / Keine), 90°-Rotation in beide Richtungen, Löschen-mit-Undo, „💾 Speichern (PDF)"-Knopf der die Operationen verkettet anwendet und das Ergebnis als `<name>_edited.pdf` herunterlädt. Neuer JS-Helfer `pageboundShortcuts.downloadBytes(filename, base64, mimeType)` produziert einen byte-perfekten Blob-Download (für Binär-Dateien, im Gegensatz zu `downloadFile`, das text-kodiert ist). Nav-Link „PDF-Werkzeuge" in der Sidebar.
- **FA-023 Merge + FA-024 Split** im `/tools`: „📎 Anhängen"-Button (gesperrt während pending Edits) lädt eine zweite PDF, mergt sie sofort hinter die aktuelle (`Manipulator.MergeAsync`), lädt das gemergte Resultat neu und rendert die zusätzlichen Thumbnails. Pro Seitenkarte ein dezenter ✂-Toggle (nur bei Hover sichtbar, dauerhaft aktiv wenn gesetzt) markiert „vor dieser Seite splitten"; visueller Accent-Strich links neben der Karte zeigt den Trennpunkt an. Save-Button-Label wechselt automatisch zu „💾 Speichern (N Teile)" und erzeugt N+1 Teil-PDFs als `<name>_part1.pdf`, `_part2.pdf`, … (jeweils binärer Download). Rotation und Löschung werden vor dem Split angewendet, Split-Punkte beziehen sich auf die fertige Reihenfolge nach Löschungen.
- **FA-015 Signatur erweitert**: Signer-Daten-Panel (Name + E-Mail + Begründung + Ort) wird in IndexedDB persistiert und beim signierten Speichern in das PDF-Info-Dictionary als `/Author` + custom Keys `/Pagebound.Signature.N.*` geschrieben. Signatur lässt sich per ↘-Handle resizen, nicht mehr nur verschieben. Signiert-speichern läuft über eine neue `pdf-lib`-JS-Bridge (`pageboundPdfManipulator.embedSignatures`) statt PdfSharpCore — letzteres crasht in Blazor WASM auf `MD5.Create()` im SecurityHandler-Konstruktor; pdf-lib hat diese Abhängigkeit nicht. `JsPdfLibManipulator` wickelt Embed in JS ab, andere Operationen delegieren weiter an `PdfSharpManipulator`.
- **FA-013 Stift + FA-014 Formen**: Freihand-Zeichnen (Pen) und Rechteck/Pfeil/Linie. `InkAnnotation` speichert Strokes als 0..1-Punktfolgen, `ShapeAnnotation` Start/End-Punkte + Form-Typ. Live-Capture läuft in JS (`pageboundShortcuts.startDrawingCapture`) auf einer eigenen SVG-Overlay-Layer; pointer-events bleiben auf der C#-Page-Seite stabil. Toolbar bekommt 3 Farben × 3 Stärken; gerenderte Strokes haben einen transparenten Hit-Helper-Stroke (0.024 Page-Fraction) als bequeme Klick-Fläche zum Löschen.
- **FA-100 + FA-101 Theme + i18n**: `ThemeService` setzt `data-theme` auf `<html>` und persistiert in localStorage (Tailwind v4 mit CSS-Variablen für Light/Dark/Sepia/Auto liegt schon im Stylesheet). `LocalizationService` lädt JSON-Bundles aus `wwwroot/resources/`, mit Token-Interpolation per `{name}`. Sprach-Toggle macht Hard-Reload (`window.location.reload`) — einfacher als jede Komponente eigene Subscriptions zu lassen. Pre-Boot-Inline-Script in `index.html` setzt Theme + Sprach-Attribute vor Blazor-Start, damit's keinen FOUC gibt. Service-Worker bekommt `skipWaiting()` + `clients.claim()` und liefert `/resources/*.json` jetzt vom Netz (sonst überschreibt das offline-cache die Sprach-Bundle-Updates). Alle UI-Strings im Reader, PdfTools, MainLayout, Home, NotFound auf `L.T()` migriert.
- **FA-070..073 Sidecar Export/Import**: `JsonSidecarService` serialisiert das existing `Sidecar`-Record (Schema 1.0) zu pretty-printed JSON. Reader bekommt zwei Buttons neben dem Markdown-Export: „💾 Sidecar speichern" lädt `<pdf>.pagebound.json` runter mit allen Annotationen + Signer + PDF-Hash, „📂 Sidecar laden" liest ein JSON zurück und fügt seine Annotationen zur aktuellen Session hinzu. Import ist additiv (Add-only, keine destruktive Replace-Logik); bei Hash-Mismatch warnt das Status-Banner. Imports bekommen frische IDs, damit Round-Trips nicht kollidieren.
- **FA-060..064 Library Phase 1**: Neue `/library`-Page mit Such-Feld, Sort-Dropdown, Tag-Filter-Pills, Inline-Tag-Edit pro Eintrag, Lösch-Action. Jede im Reader oder PdfTools geöffnete PDF wird automatisch über `LibraryService.AddOrUpdateAsync` getrackt — Match per SHA-256-Hash, damit dieselbe Datei unter anderem Namen denselben Eintrag aktualisiert. MainLayout-Sidebar zeigt die 5 zuletzt geöffneten Einträge mit Link zur vollständigen Ansicht. Persistenz pro Eintrag unter `library:entry:{id}` im IndexedDB; ein eigener Index-Key (`library:_index`) für schnelle Iteration.
- **FA-007 Thumbnails-Sidebar**: Neuer „🖼 Seiten"-Toggle neben dem Outline-Switch öffnet eine linke Sidebar, die Seiten-Vorschauen lazy bei 0.35×-Scale rendert. Aktive Seite ist umringt, Klick springt dahin. Render läuft in einer `Task.Yield`-Schleife, damit die UI auch bei 200+ Seiten reaktiv bleibt; `CancellationTokenSource` bindet den Lifecycle an „User schließt die Sidebar" / „neue PDF geladen". Outline und Thumbnails schließen einander gegenseitig (1280er-Layouts würden mit beiden Sidebars zu eng).
- **FA-090 + FA-091 Split-View Phase 1**: ReaderPage in eine wiederverwendbare `ReaderPane`-Komponente refactored (kein `@page`, kein `PageTitle`); ReaderPage ist jetzt ein dünner Wrapper. Neue Route `/split` rendert zwei ReaderPanes nebeneinander; jede Pane hat eigenen Component-State (Handle, currentPage, Zoom, Annotations-Cache) — Navigation, Suche und Annotationen sind komplett unabhängig. Shared Services (Annotations, Library, Renderer) bleiben Scoped, der Annotation-Service trennt eh per `PdfId`. Live-Cross-Pane-Propagation + Sync-Scroll (FA-092) + Drag-Resize folgen in Phase 2.
- **FA-050 OCR Phase 1**: Tesseract.js eingebaut über einen neuen `IOcrService` (`TesseractOcrService` + JS-Bridge `pageboundOcr.recognizePage`). Erster Aufruf lädt das Sprach-Bundle `eng+deu` (~10 MB pro Sprache); danach werden Worker + Modelle für die Session gecached. Button „📖 Text erkennen" steht in der Such-Toolbar und im NoTextLayer-Hinweis. Wörter mit Confidence < 60 werden gefiltert (sonst überlagern Phantom-Detections die Page). OCR-Output landet in `_pageTextItems` und ersetzt den native Text-Layer.
- **Pixel-genauer Text-Layer** für native Text-PDFs: PDF.js' eigene `TextLayer`-Klasse rendert die Word-Spans direkt ins DOM, mit `--total-scale-factor`-CSS-Variable synchronisiert zur aktuellen Bild-Display-Breite via `ResizeObserver`. Selection-Highlights kleben damit pixel-genau am Schriftbild auch über Zoom-Stufen und Window-Resize. Eigene `pb-textLayer`-CSS-Klasse adaptiert aus pdfjs-dist's `pdf_viewer.css`. OCR-Pfad bleibt bei Razor-Markup mit `transform: scaleX(...)`-Approximation (Tesseract liefert Pixel-Bounding-Boxes, keinen TextContent-Stream).
- **FA-026 Compress**: PDF.js rendert jede Seite mit Scale 2.0 auf ein Canvas, das wird mit user-gewählter Quality (40/70/90) zu JPEG komprimiert und mit pdf-lib in eine frische PDF gepackt. Robuste Größenreduktion auf foto-/scan-heavy PDFs; Vektor-Text geht dabei verloren (Trade-off klar im Tooltip benannt). Neue Compress-Bar in PdfToolsPage mit Quality-Presets und Result-Banner für die Größen-Delta. Filename-Suffix `.compressed.pdf` damit das Original nicht überschrieben wird.
- **Home-Page überholt**: Hero mit CTA-Buttons (Reader / Library / Tools), drei USP-Cards (Signatur / Library / Markdown), Built-in-Features-Liste, Roadmap-Tabelle mit Status-Pills (Erledigt/Teilweise/Geplant) und Anti-Tracking-Hinweis. Komplett über die i18n-Bundles DE/EN.
- **FA-040 / FA-041 AcroForms ausfüllen + speichern** (Release 0.8): neuer `IPdfFormService` (`Pagebound.Core.Abstractions`) + `JsPdfFormService` (`Infrastructure/Pdf`) liest und füllt interaktive Formularfelder über zwei neue pdf-lib-Bridge-Funktionen (`pageboundPdfManipulator.getFormFields` / `.fillForm`). Unterstützt alle Standard-Feldtypen (Text, Checkbox, Radio, Dropdown, ListBox), inkl. Seitenzuordnung über die Widget-Annotationen. Im Reader öffnet ein „📝 Formular"-Toggle — nur sichtbar, wenn die PDF überhaupt Felder hat — ein Panel mit den Feldern nach Seite gruppiert (Control je Typ). Gespeichert wird wahlweise **editierbar** (Werte gesetzt, Felder bleiben ausfüllbar) oder **fixiert/flatten** (Werte eingebrannt), Ergebnis als `<name>.filled.pdf`-Download; ReadOnly-Felder werden nicht überschrieben. Domain-Records `PdfFormField`/`FormFieldValue`/`FillFormOptions` + Enums `PdfFormFieldType`/`FormSaveMode`; Unit-Tests für das Typ-Token-Mapping und die Domain-Defaults. Das interaktive In-Place-Overlay (Felder direkt im PDF) folgt als Phase 2; eine Playwright-E2E mit Fixture-Formular-PDF steht noch aus.

### Fixed
- Editor-Popover (Sticky Notes / Highlights) und Inline-Toolbar werden nicht mehr von der `<figure>` abgeschnitten: `overflow-auto` → `overflow-visible`, und die Popovers flippen automatisch ihre Position, wenn die Annotation nahe einem Seitenrand sitzt (Pin rechts ⇒ Editor links, Highlight oben ⇒ Toolbar unten, etc.).

### Changed
- Bootstrap-Assets und Demo-Pages (Counter, Weather, NavMenu) durch Tailwind-basiertes Layout ersetzt
- `MainLayout`, `Home`, `NotFound`, `index.html` auf Pagebound-Branding und deutsche Sprache umgestellt
- `_Imports.razor` um `Pagebound.Core.Abstractions` und `Pagebound.Core.Domain` erweitert

### Removed
- Bootstrap-Bibliothek in `wwwroot/lib/`
- Demo-Pages: `Counter.razor`, `Weather.razor`, `NavMenu.razor`
- Generierte `UnitTest1.cs`-Platzhalter in den drei Test-Projekten

## [0.1.0] – geplant

Erstes nutzbares Alpha-Release. Inhalt:
- PDF öffnen, anzeigen, navigieren (FA-001, FA-003)
- Zoom und Volltext-Suche (FA-004, FA-005)
- Text-Highlight in mehreren Farben (FA-010)
- Sticky Notes mit Markdown-Notizen (FA-011, FA-012)
- Sidecar-JSON-Persistenz neben der PDF (FA-070, FA-071, FA-073)
- Offline-Fähigkeit als PWA (NFA-010)
- Lighthouse-Accessibility-Score ≥ 90 (NFA-034)
